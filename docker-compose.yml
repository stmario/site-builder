services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: latch
      POSTGRES_PASSWORD: latch
      POSTGRES_DB: latch
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
